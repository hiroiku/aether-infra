# ホスト名は ~/.ssh/config のエイリアスをそのまま使う。
# 新しいサーバーを足すときは、SSH で入れる状態にしてからここに 1 行追加する。

[incus_hosts]
aether ansible_host=aether ansible_user=ubuntu

[incus_hosts:vars]
ansible_python_interpreter = /usr/bin/python3
